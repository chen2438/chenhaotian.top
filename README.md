# chenhaotian.top
chenhaotian.top Hexo website.

```bash
npm install -g hexo-cli
npm install
```

img-api.js

```bash
npm install pm2 -g
pm2 start img-api.js --name "img-api"
pm2 startup
pm2 save
```

